<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    tools:context=".MenuActivity">

    <!-- TEXTO BIENVENIDA -->
    <TextView
        android:id="@+id/tvBienvenida"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bienvenido"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Imagen arriba centrada -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginTop="20dp"
        android:src="@drawable/logo"
        android:contentDescription="Logo"
        app:layout_constraintTop_toBottomOf="@id/tvBienvenida"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Botón 1 -->
    <Button
        android:id="@+id/btnNuevoAlumno"
        android:layout_width="0dp"
        android:layout_height="180dp"
        app:cornerRadius="0dp"
        android:text="NUEVO ALUMNO"
        android:textColor="#FFFFFF"
        android:backgroundTint="#1565C0"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.65"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnListaAlumnos"/>

    <!-- Botón 2 -->
    <Button
        android:id="@+id/btnListaAlumnos"
        android:layout_width="0dp"
        android:layout_height="180dp"
        app:cornerRadius="0dp"
        android:text="LISTA ALUMNOS"
        android:textColor="#FFFFFF"
        android:backgroundTint="#6A1B9A"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="@id/btnNuevoAlumno"
        app:layout_constraintBottom_toBottomOf="@id/btnNuevoAlumno"
        app:layout_constraintStart_toEndOf="@id/btnNuevoAlumno"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Botón 3 -->
    <Button
        android:id="@+id/btnMisAlumnos"
        android:layout_width="0dp"
        android:layout_height="80dp"
        app:cornerRadius="0dp"
        android:text="MIS ALUMNOS"
        android:textColor="#FFFFFF"
        android:backgroundTint="#2E7D32"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/btnNuevoAlumno"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>